# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# opennext / cloudflare build output
/.open-next/
/.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# production
/build

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/tests/visual/__screenshots__/
/tests/visual/__review__/
/lighthouse-*.json
/lh-*.json

# lighthouse
/.lighthouseci/
/lighthouse-reports/

# environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# misc
.DS_Store
*.pem
Thumbs.db

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# typescript
*.tsbuildinfo

# Operator secrets pasted into the working tree.
# Never committed. Provider keys belong in Cloudflare secrets, not in the repo,
# and a file dropped here must not be one `git add -A` away from being public.
keys.txt
keys*.txt
*.key
secrets.txt

# private analytics exports
#
# Search Console performance data is the owner's data about their own
# property. Committing it would publish a list of every query this site is
# seen for, which is not something a public repository should carry.
/private/
